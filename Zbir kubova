#include <stdio.h>
#include <string.h>
#include <math.h>


void kub(){

    for(int i=100; i<1000;i++){
        int a= i/100;
        int b= i%100/10;
        int c= i%10;
        int k= pow(a,3)+pow(b,3)+pow(c,3);

        if(k==i){
            printf("%d \n", i);
        }
    }
}

int main(){
    kub();
}
