#include <stdio.h>
#include <string.h>

int main() {

    char a[100];
    int broj_slova=0;
    fgets(a,100,stdin);

    for(int i=0;i< strlen(a);i++){
        if(a[i+1]==' ' || a[i+1]=='\0'){
            broj_slova++;
        }
    }

    printf("%d",broj_slova);
    return 0;
}
